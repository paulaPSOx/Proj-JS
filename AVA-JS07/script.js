function navegar() {
    // Obtém o valor selecionado do menu
    var selecionado = document.getElementById('url').value;

    // Navega para a URL selecionada
    window.location.href = selecionado;
}
